% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annif_learn.R
\name{annif_learn}
\alias{annif_learn}
\title{Learn from manually indexed documents}
\usage{
annif_learn(project_id, documents, verbose = FALSE)
}
\arguments{
\item{project_id}{Character string representing the project identifier
(e.g., "yso-en", "yso-fi").}

\item{documents}{A list of documents with their correct subjects.}

\item{verbose}{Logical indicating whether to print debug information (default: FALSE)}
}
\value{
Invisibly returns TRUE if successful, FALSE otherwise. The function
primarily produces side effects (model learning) rather than returning data.
}
\description{
This function sends feedback to the Annif API to improve project models
by learning from manually assigned subjects. This helps the system
improve its suggestions over time based on human feedback.
}
\examples{
\donttest{
# Create a document with correct subjects for learning
learning_doc <- list(
  text = "A quick brown fox jumped over the lazy dog.",
  subjects = list(
    list(
      uri = "http://www.yso.fi/onto/yso/p2228",
      label = "red fox"
    ),
    list(
      uri = "http://www.yso.fi/onto/yso/p5319",
      label = "dog"
    )
  ),
  metadata = list(
    source = "example",
    language = "en"
  )
)

# Send learning feedback to the model
success <- annif_learn(
  project_id = "yso-en",
  documents = list(learning_doc)
)

if (success) {
  cat("Learning feedback successfully submitted\n")
} else {
  cat("Failed to submit learning feedback\n")
}
}

}
