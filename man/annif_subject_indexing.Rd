% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annif_subject_indexing.R
\name{annif_subject_indexing}
\alias{annif_subject_indexing}
\title{Suggest subjects for a given text using the Annif REST API}
\usage{
annif_subject_indexing(
  project_id,
  text,
  limit = 10,
  threshold = 0,
  language = NULL
)
}
\arguments{
\item{project_id}{Character string representing the project identifier
(e.g., "yso-en", "yso-fi", "hogwarts").}

\item{text}{Character string containing the input text for which subjects
are to be suggested.}

\item{limit}{Integer specifying the maximum number of results to return.
Default is 10.}

\item{threshold}{Numeric value between 0 and 1 specifying the minimum score
threshold for results. Default is 0 (return all results).}

\item{language}{Character string specifying the language of subject labels.
Default is NULL (uses project default).}
}
\value{
A tibble containing suggested subjects with the following columns:
}
\description{
This function sends a text to the Annif API for automatic subject indexing
and returns suggested subjects with their labels, URIs, scores, and notations.
}
\examples{
suggestions <- annif_subject_indexing(
  project_id = "yso-en",
  text = "Jean Sibelius orchestra music and composer"
)
print(suggestions)

}
